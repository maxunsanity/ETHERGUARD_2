import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.etherguard.prototype',
  appName: 'Etherguard 2',
  webDir: 'prototype',
  bundledWebRuntime: false
};

export default config;
